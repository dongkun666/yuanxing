# LexPrime Cases Crawler - Python dependencies
# 2026-06-28 · LexPrime self-built data infrastructure

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# Auth (Track A W2)
bcrypt==5.0.0           # 密码哈希 (cost=12)
PyJWT==2.13.0           # JWT 签发/验证 (HS256)

# Auth W3: TOTP + Email verify + License OCR
pyotp==2.9.0            # TOTP 二步验证 (RFC 6238)
qrcode[pil]==7.4.2      # TOTP QR code 生成 (绑定时显示)

# Database
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0  # SQLite async (dev/demo, 零依赖)
asyncpg==0.29.0    # PostgreSQL (生产)
psycopg2-binary==2.9.9
alembic==1.13.3

# Elasticsearch (full-text search)
elasticsearch==8.15.1
elasticsearch-async==8.15.1

# Neo4j (graph database - 股权穿透)
neo4j==5.25.0
async-neo4j==0.9.0

# Crawler
scrapy==2.11.2
scrapy-redis==0.7.3
httpx[http2]==0.27.2
aiohttp==3.10.5
beautifulsoup4==4.12.3
lxml==5.3.0
parsel==1.9.1

# Anti-ban
fake-useragent==1.5.1
rotating-proxies==1.0.10
cloudscraper==1.2.71

# Utilities
python-dotenv==1.0.1
tenacity==9.0.0
tqdm==4.66.5
rich==13.9.2
loguru==0.7.2
pandas==2.2.3
pyarrow==17.0.0  # Parquet support for cncases
chardet==5.2.0   # detect CSV encoding (cncases 是 GBK)
openpyxl==3.1.5

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2  # for API tests

# Lint / Format (dev only)
ruff==0.15.20

# ===== Skill Hub (Track E) =====
# 类案检索 Skill 依赖 (T-REF-18/22/25)
lancedb>=0.6.0          # 本地向量库 (类案 / 法规 embedding)
sentence-transformers>=2.7.0  # BGE 中文 embedding
tiktoken>=0.7.0         # Token 计数 (T-REF-25 上下文压缩)
numpy>=1.26.0           # 百分位计算 (无则走纯 Python 降级)
